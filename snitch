#!/usr/bin/env python3
"""Entry point — run directly after git clone without pip install."""
import sys
import os

sys.path.insert(0, os.path.join(os.path.dirname(os.path.realpath(__file__)), "src"))

from snitch.cli import main

if __name__ == "__main__":
    main()
